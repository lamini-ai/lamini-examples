name: Test examples
on:
  push:
    branches:
      - main

  pull_request:
    branches:
      - main

jobs:
  tests:
    uses: ./.github/workflows/run_examples.yaml
    with:
        api_url: "https://api.lamini.ai"
    secrets:
        api_key: ${{ secrets.PRODUCTION_TOKEN }}